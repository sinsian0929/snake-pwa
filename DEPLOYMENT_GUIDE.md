# 部署到 GitHub Pages 完整指南

## 📋 前置準備

1. **GitHub 帳號**（如果還沒有，請到 https://github.com 註冊）
2. **Git 已安裝**（檢查：在終端機輸入 `git --version`）

---

## 🚀 部署步驟

### 步驟 1：初始化 Git 倉庫

在專案資料夾中執行：

```bash
cd d:\AI\snake-pwa
git init
git add .
git commit -m "Initial commit: Snake Premium Game"
```

### 步驟 2：在 GitHub 創建倉庫

1. 前往 https://github.com/new
2. 倉庫名稱：`snake-pwa`（或任何你喜歡的名字）
3. 設為 **Public**（公開）
4. **不要**勾選 "Add a README file"
5. 點擊 "Create repository"

### 步驟 3：連接到 GitHub

複製 GitHub 顯示的指令，或執行：

```bash
git remote add origin https://github.com/YOUR-USERNAME/snake-pwa.git
git branch -M main
git push -u origin main
```

**注意：** 將 `YOUR-USERNAME` 替換為你的 GitHub 使用者名稱

### 步驟 4：啟用 GitHub Pages

1. 前往你的倉庫頁面
2. 點擊 **Settings**（設定）
3. 左側選單點擊 **Pages**
4. 在 "Build and deployment" 下：
   - **Source**: 選擇 "GitHub Actions"
5. 儲存設定

### 步驟 5：觸發自動部署

GitHub Actions 會在你推送程式碼時自動執行。

如果想手動觸發：
1. 前往倉庫的 **Actions** 頁籤
2. 點擊 "Deploy to GitHub Pages"
3. 點擊 "Run workflow"

---

## ✅ 完成！

部署完成後，你的遊戲將可在以下網址訪問：

```
https://YOUR-USERNAME.github.io/snake-pwa/
```

---

## 📱 分享給朋友

將上面的網址分享給朋友，他們可以：
1. 在手機瀏覽器打開
2. 加入主畫面（iOS: Safari → 分享 → 加入主畫面）
3. 離線遊玩！

---

## 🔄 更新遊戲

當你修改程式碼後，只需執行：

```bash
git add .
git commit -m "Update game features"
git push
```

GitHub Actions 會自動重新部署！

---

## ❓ 常見問題

**Q: 部署失敗怎麼辦？**
A: 檢查 Actions 頁籤的錯誤訊息，通常是 `npm ci` 失敗。確保 `package-lock.json` 已提交。

**Q: 網址 404 找不到？**
A: 等待 2-3 分鐘讓部署完成，然後重新整理頁面。

**Q: 可以使用自訂網域嗎？**
A: 可以！在 Settings → Pages → Custom domain 設定。

---

## 🎉 恭喜！

你的遊戲現在已經在全球可訪問了！
